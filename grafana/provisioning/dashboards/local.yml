apiVersion: 1

providers:
  - name: 'default'
    orgId: 1
    folder: ''
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10  # How often Grafana scans for changed dashboards
    options:
      path: /var/lib/grafana/dashboards
